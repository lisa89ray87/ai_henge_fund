name: Moomoo MCP Read-Only Connectivity

on:
  workflow_dispatch:

concurrency:
  group: moomoo-mcp-readonly
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
      MOOMOO_MCP_ENABLED: "true"
      MOOMOO_MCP_URL: "https://mcp.moomoo.com/mcp"
      MOOMOO_MCP_ACCESS_TOKEN: ${{ secrets.MOOMOO_MCP_ACCESS_TOKEN }}
      MOOMOO_VERIFY_SYMBOL: "AAPL"
      PYTHON_ENV: production
      DEBUG: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Validate Moomoo MCP secret
        shell: bash
        run: |
          if [ -z "$MOOMOO_MCP_ACCESS_TOKEN" ]; then
            echo "MOOMOO_MCP_ACCESS_TOKEN GitHub Actions secret is not configured."
            echo "Complete Moomoo MCP OAuth in a supported MCP client, then store the access token as this repository secret."
            exit 1
          fi

      - name: Verify read-only Moomoo MCP quote
        run: python scripts/verify_moomoo_mcp_readonly.py
