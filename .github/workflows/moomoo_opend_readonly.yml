name: Moomoo OpenD Read-Only Test

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: [self-hosted, Windows, X64]

    steps:
      - name: Test runner
        shell: cmd
        run: echo Moomoo OpenD workflow registered successfully
