name: Moomoo Signal Pipeline

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/paper_trade_kickstart"

concurrency:
  group: moomoo-signal-pipeline
  cancel-in-progress: false

jobs:
  signal:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 420
    env:
      DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GOOGLE_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      AI_HEDGE_FUND_TELEGRAM_BOT_TOKEN: ${{ secrets.AI_HEDGE_FUND_TELEGRAM_BOT_TOKEN }}
      AI_HEDGE_FUND_TELEGRAM_CHAT_ID: ${{ secrets.AI_HEDGE_FUND_TELEGRAM_CHAT_ID }}
      AI_HEDGE_FUND_DAY_SUMMARY_BOT_TOKEN: ${{ secrets.AI_HEDGE_FUND_DAY_SUMMARY_BOT_TOKEN }}
      AI_HEDGE_FUND_DAY_SUMMARY_CHAT_ID: ${{ secrets.AI_HEDGE_FUND_DAY_SUMMARY_CHAT_ID }}
      PYTHONUTF8: "1"
      MOOMOO_OPEND_ENABLED: "true"
      MOOMOO_READ_ONLY: "true"
      MOOMOO_OPEND_HOST: "127.0.0.1"
      MOOMOO_OPEND_PORT: "11111"
      MOOMOO_SIGNAL_CANDLES: "50"
      MOOMOO_SIGNAL_INTERVAL: "5m"
      MOOMOO_SIGNAL_MAX_AI_CANDIDATES: "10"
      MOOMOO_SIGNAL_MAX_PAPER_TRADES: "10"
      MOOMOO_SIGNAL_CYCLE_MINUTES: "10"
      MOOMOO_SIGNAL_SCAN_DELAY_SECONDS: "3.2"
      MOOMOO_SIGNAL_SUBSCRIPTION_BATCH_SIZE: "45"
      SESSION_RUN_UNTIL_CLOSE: "true"
      EXECUTE_PAPER: "true"
      MOOMOO_PAPER_TRADING_ENABLED: "true"
      MOOMOO_LIVE_TRADING_ENABLED: "false"
      AI_HEDGE_FUND_STARTING_CAPITAL: "100"
      AI_HEDGE_FUND_MAX_CAPITAL_DEPLOYED: "90"
      AI_HEDGE_FUND_RISK_PER_TRADE_PCT: "50"
      AI_HEDGE_FUND_MAX_POSITIONS: "0"
      AI_HEDGE_FUND_MAX_DAILY_LOSS: "10"
      AI_HEDGE_FUND_LIVE_TRADING_ARMED: "false"
      AI_HEDGE_FUND_ALLOW_EXTENDED_HOURS: "false"
      TRADINGAGENTS_LLM_MAX_RETRIES: "1"
      TRADINGAGENTS_LIGHTWEIGHT_PAPER_AI: "true"
      GEMINI_PAPER_AI_MODEL: "gemini-3.1-flash-lite"
      GEMINI_PAPER_AI_TIMEOUT_SECONDS: "45"
      DEBUG: "false"
      PYTHON_ENV: production

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Discover installed Python 3.12
        shell: powershell
        run: |
          Write-Host "Runner identity:"
          whoami
          Write-Host "USERPROFILE=$env:USERPROFILE"

          $candidates = New-Object System.Collections.Generic.List[string]

          # Prefer the Windows Python launcher when available.
          $py = Get-Command py.exe -ErrorAction SilentlyContinue
          if ($py) {
            try {
              $launcherCandidates = & $py.Source -3.12 -c "import sys; print(sys.executable)" 2>$null
              foreach ($candidate in $launcherCandidates) {
                if ($candidate -and (Test-Path $candidate)) {
                  $candidates.Add($candidate.Trim())
                }
              }
            } catch {
              Write-Host "Python launcher could not resolve Python 3.12. Continuing with filesystem discovery."
            }
          }

          # Common per-machine and per-user Python 3.12 locations.
          $roots = @(
            "C:\Python312",
            "C:\Program Files\Python312",
            "C:\Program Files (x86)\Python312",
            "$env:LOCALAPPDATA\Programs\Python\Python312",
            "$env:USERPROFILE\AppData\Local\Programs\Python\Python312"
          )
          foreach ($root in $roots) {
            $candidates.Add((Join-Path $root "python.exe"))
          }

          # Also inspect PATH for a python.exe whose version is exactly 3.12.x.
          $pathPython = Get-Command python.exe -ErrorAction SilentlyContinue
          if ($pathPython) {
            $candidates.Add($pathPython.Source)
          }

          $python = $null
          foreach ($candidate in ($candidates | Select-Object -Unique)) {
            if (-not (Test-Path $candidate)) { continue }
            try {
              $version = (& $candidate --version 2>&1 | Out-String).Trim()
              Write-Host "Checking $candidate -> $version"
              $majorMinor = & $candidate -c "import sys; print(f'{sys.version_info.major}.{sys.version_info.minor}')" 2>$null
              if ($LASTEXITCODE -eq 0 -and $majorMinor.Trim() -eq "3.12") {
                $python = (Resolve-Path $candidate).Path
                break
              }
            } catch {
              Write-Host "Unable to execute $candidate"
            }
          }

          if (-not $python) {
            Write-Error "No installed Python 3.12 interpreter was found on the self-hosted runner."
            exit 1
          }

          Write-Host "Using Python 3.12: $python"
          & $python --version
          & $python -c "import sys; print(sys.executable); print('UTF-8 mode:', sys.flags.utf8_mode)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          $pythonDir = Split-Path $python
          "PYTHON_EXECUTABLE=$python" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "PYTHON_ROOT=$pythonDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "$pythonDir" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          "$(Join-Path $pythonDir 'Scripts')" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Verify discovered Python
        shell: cmd
        run: |
          echo PYTHON_EXECUTABLE=%PYTHON_EXECUTABLE%
          echo PYTHON_ROOT=%PYTHON_ROOT%
          "%PYTHON_EXECUTABLE%" --version
          "%PYTHON_EXECUTABLE%" -c "import sys; assert sys.version_info[:2] == (3,12); print(sys.executable)"

      - name: Install project
        shell: cmd
        run: |
          "%PYTHON_EXECUTABLE%" -m pip install --disable-pip-version-check -e .

      - name: Validate required secrets
        shell: cmd
        run: |
          if "%DATABASE_URL%"=="" (
            echo ERROR: NEON_DATABASE_URL is not configured.
            exit /b 1
          )
          if "%OPENAI_API_KEY%"=="" if "%GEMINI_API_KEY%"=="" (
            echo ERROR: OPENAI_API_KEY or GEMINI_API_KEY is required.
            exit /b 1
          )
          if "%AI_HEDGE_FUND_TELEGRAM_BOT_TOKEN%"=="" (
            echo ERROR: AI_HEDGE_FUND_TELEGRAM_BOT_TOKEN is not configured.
            exit /b 1
          )
          if "%AI_HEDGE_FUND_TELEGRAM_CHAT_ID%"=="" (
            echo ERROR: AI_HEDGE_FUND_TELEGRAM_CHAT_ID is not configured.
            exit /b 1
          )
          echo Required secrets are configured.
          echo Telegram notifier configuration is present.

      - name: Diagnose AI providers
        shell: cmd
        run: |
          "%PYTHON_EXECUTABLE%" scripts\diagnose_ai_providers.py

      - name: Run Moomoo OpenD universe paper pipeline for full regular session
        shell: cmd
        env:
          # OpenAI currently has no remaining credits. Leave the secret configured
          # for diagnostics, but hide it from the paper runtime so Gemini is used
          # as the primary provider without an unnecessary 429 fallback attempt.
          OPENAI_API_KEY: ""
        run: |
          echo MOOMOO_SIGNAL_UNIVERSE=default broad US universe
          echo MOOMOO_SIGNAL_CANDLES=%MOOMOO_SIGNAL_CANDLES%
          echo MOOMOO_SIGNAL_INTERVAL=%MOOMOO_SIGNAL_INTERVAL%
          echo MOOMOO_SIGNAL_MAX_AI_CANDIDATES=%MOOMOO_SIGNAL_MAX_AI_CANDIDATES%
          echo MOOMOO_SIGNAL_MAX_PAPER_TRADES=%MOOMOO_SIGNAL_MAX_PAPER_TRADES%
          echo MOOMOO_SIGNAL_CYCLE_MINUTES=%MOOMOO_SIGNAL_CYCLE_MINUTES%
          echo MOOMOO_SIGNAL_SCAN_DELAY_SECONDS=%MOOMOO_SIGNAL_SCAN_DELAY_SECONDS%
          echo MOOMOO_SIGNAL_SUBSCRIPTION_BATCH_SIZE=%MOOMOO_SIGNAL_SUBSCRIPTION_BATCH_SIZE%
          echo SESSION_RUN_UNTIL_CLOSE=%SESSION_RUN_UNTIL_CLOSE%
          echo EXECUTE_PAPER=%EXECUTE_PAPER%
          echo MOOMOO_PAPER_TRADING_ENABLED=%MOOMOO_PAPER_TRADING_ENABLED%
          echo MOOMOO_LIVE_TRADING_ENABLED=%MOOMOO_LIVE_TRADING_ENABLED%
          echo TRADINGAGENTS_LIGHTWEIGHT_PAPER_AI=%TRADINGAGENTS_LIGHTWEIGHT_PAPER_AI%
          echo GEMINI_PAPER_AI_MODEL=%GEMINI_PAPER_AI_MODEL%
          echo GEMINI_PAPER_AI_TIMEOUT_SECONDS=%GEMINI_PAPER_AI_TIMEOUT_SECONDS%
          echo AI_HEDGE_FUND_STARTING_CAPITAL=%AI_HEDGE_FUND_STARTING_CAPITAL%
          echo AI_HEDGE_FUND_MAX_CAPITAL_DEPLOYED=%AI_HEDGE_FUND_MAX_CAPITAL_DEPLOYED%
          echo AI_HEDGE_FUND_RISK_PER_TRADE_PCT=%AI_HEDGE_FUND_RISK_PER_TRADE_PCT%
          echo AI_HEDGE_FUND_MAX_POSITIONS=%AI_HEDGE_FUND_MAX_POSITIONS%
          echo AI_HEDGE_FUND_MAX_DAILY_LOSS=%AI_HEDGE_FUND_MAX_DAILY_LOSS%
          echo AI_HEDGE_FUND_LIVE_TRADING_ARMED=%AI_HEDGE_FUND_LIVE_TRADING_ARMED%
          echo AI_HEDGE_FUND_ALLOW_EXTENDED_HOURS=%AI_HEDGE_FUND_ALLOW_EXTENDED_HOURS%
          "%PYTHON_EXECUTABLE%" scripts\run_moomoo_signal_pipeline.py

      - name: Send paper day summary to dedicated Telegram
        if: always()
        shell: cmd
        run: |
          "%PYTHON_EXECUTABLE%" scripts\send_paper_day_summary.py
